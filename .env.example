# 服务端口（默认 8000）
PORT=8000
# 监听地址（默认 127.0.0.1；服务器部署可改 0.0.0.0）
HOST=127.0.0.1
# 服务端行情缓存 TTL（秒，默认 20，防止 Yahoo 限频）
CACHE_TTL=20
# 前端轮询间隔（毫秒）：开盘相关时段 / 休市时段
FRONTEND_REFRESH_OPEN=30000
FRONTEND_REFRESH_CLOSED=300000
